#!/usr/bin/env bash
#MISE alias="ct"
#MISE description="Build and test chezmoi in a container"
#USAGE arg "<name>" help="Distro name(s), comma-separated, or 'all'"
#USAGE arg "[subcommand]" help="build|sync|bootstrap|check|update|test|exec|shell|run" default="run"
#USAGE arg "[args]..." help="Extra arguments passed to the subcommand"

# https://www.chezmoi.io/reference/commands/docker/

set -euo pipefail

tag_prefix="${PWD##*/}"

usage() {
  cat >&2 <<'EOF'
Usage: container <name>[,<name>...] [subcommand] [args...]
       container all [subcommand]

Subcommands:
  build      Build the image and create/start the container
  sync       Wipe and re-copy source files into the container
  bootstrap  Run chezmoi bootstrap inside the container
  check      Run script/check inside the container
  update     Run script/update inside the container
  test       Run script/test inside the container
  exec       Run a command in the container (non-interactive)
  shell      Open an interactive login shell
  run        Full pipeline: build, sync, bootstrap, check, update, test (default)
EOF
  exit 1
}

container_status() {
  $CONTAINER_PROVIDER container inspect --format '{{.State.Status}}' "$1" 2>/dev/null || echo missing
}

# Sets: image_name, image_version, image_name_arg, image_version_arg, user, home, prefix, chezmoi_root, container
resolve() {
  local spec="$1"
  image_name="${spec%:*}"
  image_version="${spec#*:}"
  [ "$image_version" = "$spec" ] && image_version=latest

  image_name_arg="$image_name"
  image_version_arg="$image_version"
  prefix=""

  case "$image_name" in
  archlinux)
    [ "$(uname -m)" = arm64 ] && image_name_arg=docker.io/ogarcia/archlinux
    ;;
  macos)
    image_name_arg=docker.io/sickcodes/docker-osx
    ;;
  termux)
    image_name_arg=docker.io/termux/termux-docker
    image_version_arg=x86_64
    [ "$(uname -m)" = arm64 ] && image_version_arg=aarch64
    prefix=/data/data/com.termux/files
    ;;
  esac

  user="test"
  home="/home/$user"
  [ "$image_name" = macos ] && home="/Users/$user"
  if [ "$image_name" = termux ]; then
    home="$prefix/home"
    user=root
  fi
  chezmoi_root="$home/.local/share/chezmoi"
  container="$tag_prefix-$image_name-$image_version"
}

exec_as() {
  local flags=()
  if [ -t 0 ] && [ -t 1 ]; then
    flags+=(--tty --interactive)
  fi
  $CONTAINER_PROVIDER exec "${flags[@]}" --user="$user" --workdir="$chezmoi_root" \
    --env=PATH="$home/.local/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
    "$container" "$@"
}

cmd_build() {
  resolve "$1"

  $CONTAINER_PROVIDER build . \
    --file=Containerfile \
    --build-arg=IMAGE_NAME="$image_name_arg" \
    --build-arg=IMAGE_VERSION="$image_version_arg" \
    --tag="$container"

  $CONTAINER_PROVIDER container inspect "$container" >/dev/null 2>&1 ||
    $CONTAINER_PROVIDER create --name="$container" "$container"

  [ "$(container_status "$container")" = running ] ||
    $CONTAINER_PROVIDER start "$container"

  if [ "$image_name" = macos ]; then
    $CONTAINER_PROVIDER run -it --name="$container" \
      -device /dev/kvm \
      -p 50922:10022 \
      -p 22222:22 \
      -p 5999:5999 \
      -v /tmp/.X11-unix:/tmp/.X11-unix \
      -e "DISPLAY=:1" \
      -e AUDIO_DRIVER=pa,server=unix:/tmp/pulseaudio.socket \
      -v "/run/user/$(id -u)/pulse/native:/tmp/pulseaudio.socket" \
      -e MASTER_PLIST_URL="https://raw.githubusercontent.com/sickcodes/osx-serial-generator/master/config-custom.plist" \
      -e EXTRA="-vnc 0.0.0.0:99,password=off" \
      -e SHORTNAME=tahoe \
      "$container"
    return
  fi

  status="$(container_status "$container")"
  if [ "$status" != running ]; then
    echo >&2 "container $container is not running (status: $status)"
    exit 1
  fi

  if [ "$image_name" != termux ]; then
    $CONTAINER_PROVIDER exec "$container" sh -euxc "
      until id -u $user >/dev/null 2>&1; do
        echo 'Waiting for $user user...'
        sleep 1
      done
    "
  fi
}

cmd_sync() {
  resolve "$1"

  # FIXME: stale files cleanup
  # $CONTAINER_PROVIDER exec "$container" rm -rf "$chezmoi_root"
  $CONTAINER_PROVIDER exec "$container" mkdir -p "$chezmoi_root"

  for f in .chezmoiroot .chezmoiversion .pre-commit-config.yaml mise.toml home script test; do
    $CONTAINER_PROVIDER container cp "$f" "$container:$chezmoi_root/"
  done
  for d in test_helper test_support; do
    [ -d "$d" ] && $CONTAINER_PROVIDER container cp "$d" "$container:$chezmoi_root/"
  done

  $CONTAINER_PROVIDER exec "$container" chown -R "$user:$user" "$home"
}

cmd_bootstrap() {
  resolve "$1"

  if [ "$image_name" = termux ]; then
    $CONTAINER_PROVIDER exec "$container" \
      sh -euxc 'ln -sf ${PREFIX}/etc/termux/mirrors/default ${PREFIX}/etc/termux/chosen_mirrors'
    $CONTAINER_PROVIDER exec --user=system "$container" \
      sh -euxc 'pkg update --quiet && pkg install --quiet --yes chezmoi proot'
    $CONTAINER_PROVIDER exec \
      --env=CHEZMOI_UPGRADE=true \
      --env=CI=true \
      --user="$user" \
      --workdir="$chezmoi_root" \
      "$container" \
      proot \
      -b "$prefix/usr/etc/resolv.conf:/etc/resolv.conf" \
      -b "$prefix/usr/etc/tls:/etc/ssl" \
      ./script/bootstrap --verbose
  else
    $CONTAINER_PROVIDER exec \
      --env=CHEZMOI_UPGRADE=true \
      --env=CI=true \
      --user="$user" \
      --workdir="$chezmoi_root" \
      "$container" ./script/bootstrap --verbose
  fi
}

cmd_check() {
  resolve "$1"
  exec_as ./script/check
}

cmd_update() {
  resolve "$1"
  exec_as ./script/update
}

cmd_test() {
  resolve "$1"
  shift
  exec_as ./script/test "$@"
}

cmd_exec() {
  resolve "$1"
  shift
  exec_as "$@"
}

cmd_shell() {
  resolve "$1"
  shift
  [ $# -eq 0 ] && set -- bash -l
  exec_as "$@"
}

cmd_pipeline() {
  resolve "$1"
  exec_as mise trust --yes
  exec_as mise run pipeline
}

cmd_run() {
  cmd_build "$1" || return $?
  cmd_sync "$1" || return $?
  cmd_bootstrap "$1" || return $?
  cmd_pipeline "$1" || return $?
}

run_one() {
  local name="$1"
  shift
  case "$SUBCMD" in
  build) cmd_build "$name" ;;
  sync) cmd_sync "$name" ;;
  bootstrap) cmd_bootstrap "$name" ;;
  check) cmd_check "$name" ;;
  update) cmd_update "$name" ;;
  test) cmd_test "$name" "$@" ;;
  exec) cmd_exec "$name" "$@" ;;
  shell) cmd_shell "$name" "$@" ;;
  pipeline) cmd_pipeline "$name" ;;
  run) cmd_run "$name" ;;
  *)
    echo >&2 "Unknown subcommand: $SUBCMD"
    usage
    ;;
  esac
}

SUBCMD=run

main() {
  [ $# -eq 0 ] && usage

  CONTAINER_PROVIDER="${CONTAINER_PROVIDER:?CONTAINER_PROVIDER not set}"

  local NAME="$1"
  shift
  SUBCMD="${1:-run}"
  if [ $# -gt 0 ]; then
    shift
  fi

  if [ "$NAME" = all ]; then
    NAME=alpine,archlinux,debian,ubuntu,fedora
    [ "$CONTAINER_PROVIDER" != docker ] && NAME=$NAME,termux
  fi

  local extra=("$@")
  # local failed=()

  for name in ${NAME//,/ }; do
    local rc=0
    time run_one "$name" "${extra[@]}" || rc=$?
    # [ $rc -ne 0 ] && failed+=("$name")
    if [ $rc -ne 0 ]; then
      echo >&2 "FAILED: $name"
      exit 1
    fi
  done

  # if [ ${#failed[@]} -gt 0 ]; then
  #   echo >&2 "FAILED: ${failed[*]}"
  #   exit 1
  # fi
}

if [[ "${BASH_SOURCE[0]}" == "$0" ]]; then
  main "$@"
fi
