#!/bin/sh
#MISE description="Container entrypoint: provision dev user and keep running"
#MISE hide=true

set -eu

# Verify running in a container to prevent accidental host execution
if [ ! -f /.dockerenv ] && [ ! -f /run/.containerenv ] && [ ! -d /proc/1/cgroup ]; then
  echo "ERROR: This entrypoint must only run inside a container" >&2
  echo "Refusing to execute on host system" >&2
  exit 1
fi

if [ $# -ne 0 ]; then
  echo "entrypoint: unexpected arguments: $*" >&2
  exit 2
fi

keep_running() {
  if [ -f /entrypoint.sh ]; then
    exec /entrypoint.sh tail -f /dev/null
  else
    exec tail -f /dev/null
  fi
}

# Termux manages its own user setup
if [ -n "${PREFIX:-}" ]; then
  echo "entrypoint: termux, skipping provisioning" >&2
  keep_running
fi

user="${DEV_USER:-test}"

install_packages() {
  if command -v apk >/dev/null; then
    apk update --quiet
    apk add bash chezmoi curl git shadow sudo unzip
  elif command -v apt-get >/dev/null; then
    apt-get update --quiet
    DEBIAN_FRONTEND=noninteractive apt-get install --no-install-recommends --quiet --yes \
      ca-certificates curl git locales sudo unzip
  elif command -v dnf >/dev/null; then
    dnf install --assumeyes --quiet chezmoi git sudo which unzip
  elif command -v pacman >/dev/null; then
    pacman --sync --needed --noconfirm --quiet --refresh chezmoi git sudo which
  elif command -v zypper >/dev/null; then
    zypper --quiet --non-interactive install chezmoi curl git sudo which unzip
  else
    echo "entrypoint: unsupported package manager" >&2
    exit 1
  fi
}

create_user() {
  id -u "$user" >/dev/null 2>&1 && return
  echo "entrypoint: creating user $user" >&2
  if command -v useradd >/dev/null; then
    useradd --create-home --shell=/bin/bash "$user"
  else
    # alpine: -D = no password, -s = shell
    adduser -Ds /bin/bash "$user"
  fi
}

grant_sudo() {
  # sudoers drop-in works on all distros without group name guessing
  echo "$user ALL=(root) NOPASSWD:ALL" >"/etc/sudoers.d/$user"
  chmod 0440 "/etc/sudoers.d/$user"
}

verify() {
  if [ "$(id -u "$user")" -eq 0 ]; then
    echo "entrypoint: $user has UID 0, aborting" >&2
    exit 1
  fi
  if [ ! -f "/etc/sudoers.d/$user" ]; then
    echo "entrypoint: sudoers drop-in missing for $user, aborting" >&2
    exit 1
  fi
}

install_packages
create_user
grant_sudo
verify

keep_running
