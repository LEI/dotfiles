#!/usr/bin/env bash
#MISE description="Render chezmoi templates into output/prek"

set -euo pipefail

render_dir="$PWD/output/prek"
rm -rf "$render_dir"
mkdir -p "$render_dir"

chezmoi-render "$render_dir" "$@" >&2

printf 'disable=SC1090,SC2034,SC2154,SC2248\n' >"$render_dir/.shellcheckrc"
