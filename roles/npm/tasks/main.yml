---
# npm config prefix:
# docs.npmjs.com/getting-started/fixing-npm-permissions
# github.com/sindresorhus/guides/blob/master/npm-global-without-sudo.md

# - name: Set npm configuration prefix
#   shell: "npm config set prefix {{ npm_prefix }}"
#   changed_when: false

# - name: "Check {{ npm_prefix.stdout }}/lib/node_modules permissions"
#   file:
#     path: "{{ npm_prefix.stdout }}/lib/node_modules"
#     owner: "{{ ansible_user_id }}"
#     # recurse: yes
#   when: npm_prefix.stdout is defined

- name: "Install {{ npm_packages | length }} npm packages"
  npm:
    name: "{{ item.name | default(item) }}"
    version: "{{ item.version | default(omit) }}"
    global: "{{ item.global | default(npm_global) }}"
    state: "{{ item.state | default(npm_state) }}"
    path: "{{ item.path | default(omit) }}"
  with_items: "{{ npm_packages }}"
  when: npm_install
  # become: "{{ 'yes' if ansible_os_family in ['Debian'] else 'no' }}"
