---
dependencies:
  # - role: debops.nodejs
  #   when: "ansible_os_family == 'Debian'"
  # - role: brew
  #   packages:
  #     - node
  #   when: "ansible_os_family == 'Darwin'"
  - role: templates
    role_dir: npm
