---
dependencies:

#   - role: package
#     packages:
#       - node
#     when: "ansible_os_family == 'Darwin'"

#   - role: nodejs
#     when: "ansible_os_family == 'Debian'"

  - role: dot.template
    dot_files:
      npm:
        prefix: .
