---
dependencies:

  - role: package
    packages:
      - "{{ 'git-core' if ansible_distribution == 'Debian' else 'git' }}"

  # - role: dotfiles
  #   paths:
  #     - name: git
  #       prefix: .
  #     - name: git
  #       prefix: .
  #       template: yes
