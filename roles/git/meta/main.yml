---
dependencies:

  - role: package
    packages:
      - "{{ 'git-core' if ansible_distribution == 'Debian' else 'git' }}"

  - role: symlink
    dot_files:
      git:
        prefix: .

  - role: template
    dot_files:
      git:
        prefix: .
