---
dependencies:

  - role: package
    packages:
      - tmux
  #   when: "ansible_os_family == 'Debian'"

  # - role: brew
  #   packages:
  #     - tmux
  #     # - reattach-to-user-namespace
  #   when: "ansible_os_family == 'Darwin'"

  - role: symlink
    dot_files:
      tmux:
        dest: .tmux

  # Must run after symlink
  - role: template
    dot_files:
      tmux:
        dest: .tmux
