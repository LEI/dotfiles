---
dependencies:

  - role: package
    packages:
      - tmux

  #   when: "ansible_os_family == 'Debian'"
  # - role: brew
  #   packages:
  #     - tmux
  #     # - reattach-to-user-namespace
  #   when: "ansible_os_family == 'Darwin'"
  #
  - role: template
    role_dir: tmux
