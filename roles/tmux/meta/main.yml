---
dependencies:

  - role: package
    packages:
      - tmux
  #   when: "ansible_os_family == 'Debian'"

  # - role: brew
  #   packages:
  #     - tmux
  #     # - reattach-to-user-namespace
  #   when: "ansible_os_family == 'Darwin'"

  - role: dotfiles
    paths:
      - name: tmux
        dest: .tmux
      - name: tmux
        dest: .tmux
        pattern: "tmux.conf.j2"
        template: yes
