---
dependencies:
  - role: apt
    packages:
      - tmux
    when: "ansible_os_family == 'Debian'"
  - role: brew
    packages:
      - tmux
      # - reattach-to-user-namespace
    when: "ansible_os_family == 'Darwin'"
  - role: templates
    role_dir: tmux
