---
dependencies:

  - role: symlink
    dot_files:
      bin:
        src: bin # ~/.dotfiles/bin
        dest: bin # ~/bin
      bash:
        prefix: .

  - role: get-url
    files: "{{ bash_download }}"
