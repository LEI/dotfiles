---
dependencies:

  - role: homebrew-cask
    homebrew_cask_apps:
      - hammerspoon
    # when: "ansible_os_family == 'Darwin'"

  - role: dotfiles
    role_dir: roles/system
    paths:
      - name: hammerspoon
        dest: .hammerspoon
