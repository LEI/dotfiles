---
dependencies:

  - role: package
    packages:
      - name: vim
        install_options: "{{ vim_brew_options }}"

  - role: template
    dot_files:
      vim:
        dest: .vim/before

  - role: symlink
    dot_files:
      vim:
        dest: .vim
