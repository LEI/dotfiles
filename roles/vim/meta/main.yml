---
dependencies:

  - role: package
    packages: "{{ vim_packages }}"

  # - role: pacman-yaourt
  #   pacman_yaourt_packages:
  #     - vim-youcompleteme-git
  #   when: "ansible_os_family == 'Archlinux'"

  - role: template
    dot_files:
      vim:
        dest: .vim
        pattern: "*.j2"

  - role: symlink
    dot_files:
      vim:
        dest: .vim
