---
dependencies:

  - role: package
    packages: "{{ vim_packages }}"

  # - role: package
  #   packages:
  #     - vim-python # vim-nox?
  #   when: "ansible_os_family == 'Debian'"

  - role: template
    dot_files:
      vim:
        dest: .vim/before

  - role: symlink
    dot_files:
      vim:
        dest: .vim
