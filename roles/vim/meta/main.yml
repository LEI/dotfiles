---
dependencies:

  - role: package
    packages: "{{ vim_packages }}"

  # - role: yaourt
  #   yaourt_packages:
  #     - vim-youcompleteme-git
  #   when: "ansible_os_family == 'Archlinux'"

  - role: template
    dot_files:
      vim:
        dest: .vim

  - role: symlink
    dot_files:
      vim:
        dest: .vim
