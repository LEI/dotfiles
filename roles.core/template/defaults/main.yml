---
# The role directory variable must be defined with the role itself
# For example as a dependency in meta/main.yml
# role_dir:
backup_enable: yes
backup_templates: yes
log_enable: no

templates:
  prefix:
  src:
  dest:
