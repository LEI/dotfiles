# Ansible dotfiles

## Install

    bin/dot

## tmux

    C-a (prefix) + I

## TODO

    - [ ] OS X role (defaults, applescript)
    - [ ] Homebrew casks cli (skipped?)
    - [ ] SSH (ansible vault?)
    - [ ] irssi / mutt
