# vi: ft=dosini

[local]
localhost

[local:vars]
# ansible_host=localhost
ansible_connection=local
# Archlinux uses python3 by default
ansible_python_interpreter=python2

# [remote]
# homestead
# pi ansible_host=192.168.1.20 ansible_user=pi

# Default: vagrant@127.0.0.1:2222 (cf. group_vars/vagrant)
# [vagrant]
# homestead
