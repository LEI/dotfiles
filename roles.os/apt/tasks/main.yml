---
- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: "{{ apt_cache_time | default(omit) }}"
  when: packages_update
  become: yes

- name: Upgrade apt packages
  apt:
    upgrade: "{{ apt_upgrade }}"
  when: packages_upgrade
  become: yes
