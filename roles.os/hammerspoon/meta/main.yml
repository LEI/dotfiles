---
dependencies:

  - role: cask
    homebrew_cask_apps:
      - hammerspoon
    # when: "ansible_os_family == 'Darwin'"

  - role: dot.symlink
    dot_roles_dir: roles.os
    dot_files:
      hammerspoon:
        dest: .hammerspoon
