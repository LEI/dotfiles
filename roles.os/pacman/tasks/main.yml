---
- name: Update pacman cache
  pacman:
    update_cache: yes
  when: packages_update
  become: yes

- name: Upgrade pacman packages
  pacman:
    upgrade: yes
  when: packages_upgrade
  become: yes

# AUR?
# - name: "Check {{ pacman_conf }}"
#   lineinfile:
#     dest: "{{ pacman_conf }}"
#     state: present
#     line: "{{ item }}"
#   with_items:
#     - "[archlinuxfr]"
#     - "SigLevel = Never"
#     - "Server = http://repo.archlinux.fr/$arch"
