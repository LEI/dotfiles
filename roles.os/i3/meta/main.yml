---
dependencies:

  - role: package
    packages:
      - i3
      - rofi
      - scrot

  - role: pacman-yaourt
    pacman_yaourt_packages:
      - i3blocks

  - role: dotfiles
    role_dir: roles.os
    paths:
      - name: i3
        dest: .config/i3
        pattern: "config"
      - name: i3
        pattern: "*.conf"
        prefix: .
      - name: xresources
        src: roles.os/i3/files/Xresources.d
        dest: .Xresources.d
