---
dependencies:

  - role: package
    packages:
      - i3
      - rofi
      - scrot

  - role: pacman-yaourt
    pacman_yaourt_packages:
      - i3blocks

  - role: dotfiles
    role_dir: roles.os
    paths:
      - name: i3
        role_dir: roles.os
        dest: .config/i3
        pattern: "config"
      - name: home
        role_dir: roles.os
        pattern: "*.conf"
        prefix: .
      - name: xresources
        src: roles.os/i3/files/Xresources.d
        dest: .Xresources.d
