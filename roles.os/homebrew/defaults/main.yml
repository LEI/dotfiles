---
packages_update: yes
packages_upgrade: yes

homebrew_install_script: "https://raw.githubusercontent.com/Homebrew/install/master/install"
homebrew_install_path: /usr/local
homebrew_creates: Cellar # Relative to install path
homebrew_install: yes
homebrew_cleanup: yes

homebrew_taps: []
homebrew_taps_state: "{{ packages_state | default('present') }}"
