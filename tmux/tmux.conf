# .tmux.conf
# vim: foldenable foldmethod=marker

# ! https://github.com/tmux-plugins/tpm

# ? https://github.com/edkolev/tmuxline.vim

# github.com/tmux/tmux/blob/master/example_tmux.conf
# github.com/demure/dotfiles/blob/master/tmux.conf
# github.com/tangledhelix/dotfiles/blob/master/tmux.conf

# Main {{{
#
# Turn on xterm key bindings so that additional function keys get escape sequences
set -g xterm-keys on

# Set colors
set -g default-terminal "xterm-256color" #xterm|screen

# Scrollback size
set -g history-limit 10000

# Don't wait for escape characters
set -sg escape-time 1
# -s 10 ?

# Locks
set -g lock-after-time 1800

# Keep windows around after they exit
#set -g remain-on-exit on

# }}}
# General {{{

setw -g utf8 on

# Start numbering at 1
set -g base-index 1
setw -g pane-base-index 1

# Automatically renumber windows
set -g renumber-windows on

# No bells at all
set -g bell-action none

# Disable window activity message
set -g visual-activity off

# Enable activity alerts
setw -g monitor-activity on
set -g message-style fg=black,bg=blue

# Window renaming
setw -g allow-rename on
setw -g automatic-rename on

# Set window title string
# #  #H  Hostname of local host
# #  #I  Current window index
# #  #P  Current pane index
# #  #S  Session name
# #  #T  Current window title
# #  #W  Current window name
set -g set-titles on
set -g set-titles-string '#S.#I.#P #W #T'

# }}}
# Windows {{{

setw -g window-status-format " #[fg=brightblack]#I#{?window_flags,#F, }#[default]#W "
setw -g window-status-style fg=brightblack,bg=black

# Current window
setw -g window-status-current-format " #[fg=brightblack]#I#{?window_flags,#F, }#[default]#W "
setw -g window-status-current-style fg=black,bg=blue #bg=colour10

# Active window
setw -g window-status-activity-style fg=white,bg=black #bg=colour10 #fg=black,bg=brightblue

# Panes
set -g pane-border-style fg=white,bg=black
set -g pane-active-border-style fg=blue,bg=black

# }}}
# Status bar {{{

set -g status on
set -g status-interval 2
set -g status-justify centre
set -g status-position bottom
set -g status-utf8 on
set -g status-keys emacs

# Lengths
set -g status-left-length 60
set -g status-right-length 80

# Styles
setw -g status-style fg=white,bg=black
setw -g status-left-style fg=brightblack,bg=black
setw -g status-right-style fg=brightblack,bg=black

# Left section
#
setw -g status-left ""
#[fg=black,bold]#(whoami)#[fg=brightblack,nobold]@
# Host
setw -ga status-left "#[fg=black,bg=blue] #H #[fg=default]"
# Session name
setw -ga status-left "#[fg=colour12,bg=colour11] #S "
# Public IP
setw -ga status-left "#[default] #(curl ipecho.net/plain;echo) #(uname -r | cut -c 1-6)"

# Right section
#
setw -g status-right ""
# Memory (npm)
setw -ga status-right "#(tmux-mem --no-color --format ':currentBytes #[fg=:color]:percent#[fg=default] |#[fg=:color]:spark#[fg=default]|') "
# CPU (npm)
setw -ga status-right "#(tmux-cpu --no-color --format ':load #[fg=:color]:percent#[fg=default] |#[fg=:color]:spark#[fg=default]|') "
# Battery (./plugins)
setw -ga status-right "|#{battery_percentage}#[fg=default]| "
# Session and panel index
setw -ga status-right "#[fg=colour12,bg=colour11] #I: #P "
# Date and time
setw -ga status-right "#[fg=black,bg=blue] %a %b %d %H:%M "
#(ifstat)
#(cpu-load 1)

# }}}
# Input {{{

# Mouse
#
set -g mouse-utf8 on
set -g mouse on

# https://github.com/tmux/tmux/issues/145
bind -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"
bind -n WheelDownPane select-pane -t= \; send-keys -M

#bind -n C-WheelUpPane select-pane -t= \; copy-mode -e \; send-keys -M
#bind -t vi-copy    C-WheelUpPane   halfpage-up
#bind -t vi-copy    C-WheelDownPane halfpage-down
#bind -t emacs-copy C-WheelUpPane   halfpage-up
#bind -t emacs-copy C-WheelDownPane halfpage-down

# Mouse ithout copy mode dragging?
# unbind -n MouseDrag1Pane
# unbind -temacs-copy MouseDrag1Pane
# Vi copypaste mode
# bind-key -t vi-copy 'v' begin-selection
# bind-key -t vi-copy 'y' copy-selection

# Key maps

# Use emacs bindings?
setw -g mode-keys emacs

# Change the prefix key to C-a
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# Switch to last window
bind a last-window

# Vim style pane navigation
unbind-key j
bind-key j select-pane -D
unbind-key k
bind-key k select-pane -U
unbind-key h
bind-key h select-pane -L
unbind-key l
bind-key l select-pane -R

# Pane resizing
bind H resize-pane -L 5
bind J resize-pane -D 5
bind K resize-pane -U 5
bind L resize-pane -R 5

# Quick pane cycling
# unbind ^A
# bind ^A select-pane -t :.+

# Some extra key bindings to select higher numbered windows
bind F1 selectw -t:10
bind F2 selectw -t:11
bind F3 selectw -t:12
bind F4 selectw -t:13
bind F5 selectw -t:14
bind F6 selectw -t:15
bind F7 selectw -t:16
bind F8 selectw -t:17
bind F9 selectw -t:18
bind F10 selectw -t:19
bind F11 selectw -t:20
bind F12 selectw -t:21

# Reload config
bind r source-file ~/.tmux.conf \; display-message " Reloaded ~/.tmux.conf"


# }}}
# Plugins {{{

# Tmux Plugin Manager
set -g @plugin 'tmux-plugins/tpm'

set -g @plugin 'battery'

# Examples:
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'github_username/plugin_name'
# set -g @plugin 'git@github.com/user/plugin'
# set -g @plugin 'git@bitbucket.com/user/plugin'

# Sessions
set -g @plugin 'tmux-plugins/tmux-resurrect'

# CPU
#set -g @plugin 'tmux-plugins/tmux-cpu'
#{cpu_icon} #{cpu_percentage}
# Battery
#set -g @plugin 'tmux-plugins/tmux-battery'
#:( 'thewtex/tmux-mem-cpu-load'

# Keep this line at the very bottom of tmux.conf
run '~/.tmux/plugins/tpm/tpm'
# }}}

# Misc {{{
#
# # Some tweaks to the status line
# #set -g status-bg green
# #set -g status-right "%H:%M"
# #set -g window-status-current-attr "underscore"
#
# # Default window title colors
# setw -g window-status-fg colour244 #base0
# setw -g window-status-bg default
# #setw -g window-status-attr dim
#
# # Active window title colors
# setw -g window-status-current-fg colour166 #orange
# setw -g window-status-current-bg default
# #setw -g window-status-current-attr bright
#
# # Pane border
# set-option -g pane-border-fg colour235 #base02
# set-option -g pane-active-border-fg colour240 #base01
#
# # Message text
# set-option -g message-bg colour235 #base02
# set-option -g message-fg colour166 #orange
#
# # Pane number display
# set-option -g display-panes-active-colour colour33 #blue
# set-option -g display-panes-colour colour166 #orange
# # Clock
# setw -g clock-mode-colour green #green
#
# # Status bar
# set-option -g status-utf8 on
# set-option -g status-bg colour235 #base02
# set-option -g status-fg colour136 #yellow
# set-option -g status-attr default
#
# set -g status-interval 1
# set -g status-justify centre # center align window list
# set -g status-left-length 20
# set -g status-right-length 140
# set -g status-left '#[fg=green]#H #[fg=black]• #[fg=green,bright]#(uname -r | cut -c 1-6)#[default]'
# set -g status-right '#[fg=green,bg=default,bright]#(tmux-mem-cpu-load) #[fg=red,dim,bg=default]#(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") #[fg=white,bg=default]%a%l:%M:%S %p#[default] #[fg=blue]%Y-%m-%d'







# Keys to toggle monitoring activity in a window, and synchronize-panes
# bind m set monitor-activity
# bind y set synchronize-panes\; display 'synchronize-panes #{?synchronize-panes,on,off}'


# # Set window split
# bind-key v split-window -h
# bind-key b split-window

# Create a single default session
# new -d -s0 -nirssi 'exec irssi'
# set -t0:0 monitor-activity on
# set  -t0:0 aggressive-resize on
# neww -d -ntodo 'exec emacs ~/TODO'
# setw -t0:1 aggressive-resize on
# neww -d -nmutt 'exec mutt'
# setw -t0:2 aggressive-resize on
# neww -d
# neww -d
# neww -d



# # https://github.com/seebi/tmux-colors-solarized/blob/master/tmuxcolors-256.conf
#
#
# set -g status-interval 1
# set -g status-justify centre # center align window list
# set -g status-left-length 20
# set -g status-right-length 140
# set -g status-left '#[fg=green]#H #[fg=black]• #[fg=green,bright]#(uname -r | cut -c 1-6)#[default]'
# set -g status-right '#[fg=green,bg=default,bright]#(tmux-mem-cpu-load) #[fg=red,dim,bg=default]#(uptime | cut -f 4-5 -d " " | cut -f 1 -d ",") #[fg=white,bg=default]%a%l:%M:%S %p#[default] #[fg=blue]%Y-%m-%d'
#
# # C-b is not acceptable -- Vim uses it
# set-option -g prefix C-a
# bind-key C-a last-window
#
#
# # Allows for faster key repetition
# set -s escape-time 0
#
# # Rather than constraining window size to the maximum size of any client
# # connected to the *session*, constrain window size to the maximum size of any
# # client connected to *that window*. Much more reasonable.
# setw -g aggressive-resize on
#
# # Allows us to use C-a a <command> to send commands to a TMUX session inside
# # another TMUX session
# bind-key a send-prefix
#
# # Activity monitoring
# setw -g monitor-activity on
# set -g visual-activity on
#
# # Vi copypaste mode
# setw -g mode-keys vi
# bind-key -t vi-copy 'v' begin-selection
# bind-key -t vi-copy 'y' copy-selection
#
# # hjkl pane traversal
# bind h select-pane -L
# bind j select-pane -D
# bind k select-pane -U
# bind l select-pane -R
#
# # set to main-horizontal, 60% height for main pane
# bind m setw main-pane-height 60\; select-layout main-horizontal
#
# bind-key C command-prompt -p "Name of new window: " "new-window -n '%%'"
#
#
# # auto window rename
# setw -g automatic-rename
#
# # color
# set -g default-terminal "screen-256color"
#
# # status bar
# set-option -g status-utf8 on
#
# # https://github.com/edkolev/dots/blob/master/tmux.conf
# # Updates for tmux 1.9's current pane splitting paths.
#
# # from powerline
# run-shell "tmux set-environment -g TMUX_VERSION_MAJOR $(tmux -V | cut -d' ' -f2 | cut -d'.' -f1 | sed 's/[^0-9]*//g')"
# run-shell "tmux set-environment -g TMUX_VERSION_MINOR $(tmux -V | cut -d' ' -f2 | cut -d'.' -f2 | sed 's/[^0-9]*//g')"
#
# # rm mouse mode fail
# if-shell '\( #{$TMUX_VERSION_MAJOR} -eq 2 -a #{$TMUX_VERSION_MINOR} -ge 1\) -o #{$TMUX_VERSION_MAJOR} -gt 2' 'set -g mouse off'
# if-shell '\( #{$TMUX_VERSION_MAJOR} -eq 2 -a #{$TMUX_VERSION_MINOR} -lt 1\) -o #{$TMUX_VERSION_MAJOR} -le 1' 'set -g mode-mouse off'
#
# # fix pane_current_path on new window and splits
# if-shell "#{$TMUX_VERSION_MAJOR} -gt 1 -o \( #{$TMUX_VERSION_MAJOR} -eq 1 -a #{$TMUX_VERSION_MINOR} -ge 8 \)" 'unbind c; bind c new-window -c "#{pane_current_path}"'
# if-shell "#{$TMUX_VERSION_MAJOR} -gt 1 -o \( #{$TMUX_VERSION_MAJOR} -eq 1 -a #{$TMUX_VERSION_MINOR} -ge 8 \)" "unbind '\"'; bind '\"' split-window -v -c '#{pane_current_path}'"
# if-shell "#{$TMUX_VERSION_MAJOR} -gt 1 -o \( #{$TMUX_VERSION_MAJOR} -eq 1 -a #{$TMUX_VERSION_MINOR} -ge 8 \)" 'unbind v; bind v split-window -h -c "#{pane_current_path}"'
# if-shell "#{$TMUX_VERSION_MAJOR} -gt 1 -o \( #{$TMUX_VERSION_MAJOR} -eq 1 -a #{$TMUX_VERSION_MINOR} -ge 8 \)" 'unbind %; bind % split-window -h -c "#{pane_current_path}"'
#
# }}}
