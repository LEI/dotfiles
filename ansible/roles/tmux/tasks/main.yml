---
- include: osx.yml
  when: ansible_os_family == 'Darwin'

- include: debian.yml
  when: ansible_os_family == 'Debian'

- include: ../../files/tasks/symlink_items.yml

# - name: Make sure .tmux directory exists
#   file:
#     path: "{{ dot_home }}/{{ paths.dst }}"
#     state: directory

# - name: Symlink tmux configuration
#   # register: files
#   with_items: "{{ symlinks }}"
#   # when: item.changed
#   notify: symlink_items
