#!/usr/bin/env bash

# dot

if [[ -z "$DOT_ROOT" ]]; then
   echo "\$DOT_ROOT is undefined"
   exit 1
fi

# Paths
cd "$DOT_ROOT"
# export DOT_ROOT=$HOME/.dotfiles
# cd $(dirname "${BASH_SOURCE[0]}")/..
# cd $(dirname "$(readlink "${BASH_SOURCE[0]}")")/..
# DOT_ROOT=$(pwd -P)
DOT_TARGET=$HOME
DOT_SCRIPT="$DOT_ROOT/script"
DOT_RC="$DOT_ROOT/.dotrc"

[[ "$0" == "$BASH_SOURCE" ]] && "$DOT_SCRIPT/dotfiles" "$@"
