# Bootstrap: lib/ contains infrastructure sourced before everything else
XDG_CONFIG_HOME="${XDG_CONFIG_HOME:-$HOME/.config}"
source "$XDG_CONFIG_HOME/sh/lib/export.sh"

if [ -d "$XDG_CONFIG_HOME/environment.d" ]; then
  export_env "$XDG_CONFIG_HOME/environment.d/"*.conf
fi

for _s in github gitlab; do
  if [ -f "$XDG_CONFIG_HOME/secrets.d/$_s.conf" ]; then
    export_env "$XDG_CONFIG_HOME/secrets.d/$_s.conf"
  fi
done
unset _s

source "$XDG_CONFIG_HOME/sh/lib/pathmunge.sh"
source "$XDG_CONFIG_HOME/sh/lib/bench.sh"

# Source local environment variables
if [ -f "$XDG_CONFIG_HOME/sh/local.sh" ]; then
  source "$XDG_CONFIG_HOME/sh/local.sh"
fi

# Non-interactive command mode; see environment.plist (BASH_ENV)
# https://github.com/neovide/neovide/discussions/3175
if [[ $- == *c* ]] && [[ $- != *i* ]]; then
  # [[ "${BASH_EXECUTION_STRING:-}" == "{{ lookPath "nvim" }} --embed -p" ]]
  # BASH_CONFIG="neovide"
  PATH="$PATH:$HOME/.local/bin"
  if [ -z "${MISE_SHELL:-}" ]; then
    PATH="$PATH:$XDG_DATA_HOME/mise/shims"
  fi
  if [ -f "$XDG_CONFIG_HOME/sh/environment.sh" ]; then
    source "$XDG_CONFIG_HOME/sh/environment.sh"
  fi
  return
fi

if [[ $- != *i* ]]; then
  # We are being invoked from a non-interactive shell.  If this
  # is an SSH session (as in "ssh host command"), source
  # /etc/profile so we get PATH and other essential variables.
  [[ -n "${SSH_CLIENT:-}" ]] && source /etc/profile
  return
fi

# Remove default prompt command
if [ -n "${PROMPT_COMMAND:-}" ] && [[ "$PROMPT_COMMAND" = *"\033]0;"* ]]; then
  export PROMPT_COMMAND=
fi

{{- $shell := get . "shell" }}
{{- if empty $shell }}
{{-   warnf "missing shell (bash or zsh)" }}
{{- end }}

shell="{{ $shell }}" # ${SHELL##*/}

if [ -z "${USER:-}" ]; then
  USER="$(id -un)"
fi

# Load environment variables before other scripts (e.g. INPUTRC before ble.sh)
if [ -f "$XDG_CONFIG_HOME/sh/environment.sh" ]; then
  source "$XDG_CONFIG_HOME/sh/environment.sh"
fi

# Feature init files; order-sensitive tools are sourced explicitly below
{{- $src := .chezmoi.sourceDir }}
{{- $dotcfg := joinPath $src "dot_config" }}
{{- $exclude := list "atuin" "bash" "carapace" "direnv" "mise" "starship" "zoxide" "zsh" }}
for _f in \
  "$XDG_CONFIG_HOME/sh/functions/"*.sh \
{{- range glob (joinPath $src "dot_*/init.sh*") }}
  {{- $name := . | dir | base | trimPrefix "dot_" }}
  "$HOME/.{{ $name }}/{{ . | base | trimSuffix ".tmpl" }}" \
{{- end }}
{{- range glob (joinPath $dotcfg "*/init.sh*") }}
  {{- $name := . | dir | base }}
  {{- if not (has $name $exclude) }}
  "$XDG_CONFIG_HOME/{{ $name }}/{{ . | base | trimSuffix ".tmpl" }}" \
  {{- end }}
{{- end }}
; do
  if [ -f "$_f" ]; then
    source "$_f"
  fi
done
unset _f

if [ -d "$HOME/bin" ]; then
  pathmunge "$HOME/bin"
fi

{{- if .features.brew }}

# Homebrew
brew_prefix="${HOMEBREW_PREFIX:-{{ .brewPrefix }}}"

if [ -x "$brew_prefix/bin/brew" ]; then
  eval "$("$brew_prefix/bin/brew" shellenv)"
fi

{{- if .features.bash }}

if [ -d "$brew_prefix/opt/bash/bin" ]; then
  pathmunge "$brew_prefix/opt/bash/bin" replace
fi
{{- end }}
{{- end }}

{{- if and (ne .osID "alpine") (ne .os "windows") }}

if [ "$shell" = bash ]; then
  # ble.sh includes its own preexec; never load both
  if [ -f "$XDG_DATA_HOME/blesh/ble.sh" ]; then
    source "$XDG_DATA_HOME/blesh/ble.sh" --attach=none --rcfile "$XDG_CONFIG_HOME/blesh/config.sh"
  elif [ -f "$XDG_DATA_HOME/bash-preexec.sh" ]; then
    source "$XDG_DATA_HOME/bash-preexec.sh"
  fi
fi
{{- end }}

{{- if .features.carapace }}

if [ -d "$XDG_CONFIG_HOME/carapace/bin" ]; then
  pathmunge "$XDG_CONFIG_HOME/carapace/bin" after
fi
{{- end }}

{{- if and .features.sheldon (lookPath "sheldon" | isExecutable) }}
# https://github.com/rossmacarthur/sheldon#loading-plugins
if [ "$shell" = bash ]; then
  export SHELDON_CONFIG_FILE="$XDG_CONFIG_HOME/sheldon/bash.toml"
fi
eval "$(sheldon source)"
{{- else }}
{{ includeTemplate "init-tools.sh.tmpl" . | trim }}
{{- end }}

if [ -f "$XDG_CONFIG_HOME/sh/aliases.sh" ]; then
  source "$XDG_CONFIG_HOME/sh/aliases.sh"
fi

# Per-shell config: .conf for variables (via export_env), .sh for shell code
for _f in "$XDG_CONFIG_HOME/$shell/"*.conf; do
  if [ -f "$_f" ]; then
    export_env "$_f"
  fi
done
for _f in "$XDG_CONFIG_HOME/$shell/"*.sh; do
  case "${_f##*/}" in init.sh) continue ;; esac
  if [ -f "$_f" ]; then
    source "$_f"
  fi
done
unset _f

if [ -f "$HOME/.${shell}rc.local" ]; then
  source "$HOME/.${shell}rc.local"
fi

# https://github.com/akinomyoga/ble.sh#13-set-up-bashrc
if [[ ${BLE_VERSION-} ]]; then
  ble-attach
fi
