# export CHEZMOI_GITHUB_ACCESS_TOKEN=""
{{- if not (or .ephemeral .headless) }}
# export GITHUB_ACCESS_TOKEN=""
{{- end }}

# typeset -A SSHCD_DIR=( [host]=/var/www/app )
# typeset -A SSHCD_LOGS=( [host]='tail -f /var/log/*.log' )
# typeset -A SSHCD_LOGS_FMT=( [host]='tspin' )
