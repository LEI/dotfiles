{{- /* chezmoi:modify-template */ -}}
{{- $lines := list
  "[[ -f ~/.profile ]] && . ~/.profile"
  "[[ -f ~/.bashrc ]] && source ~/.bashrc"
-}}
{{- includeTemplate "block-in-file.tmpl" (dict
  "chezmoi" .chezmoi
  "contents" ($lines | join "\n")
) -}}
