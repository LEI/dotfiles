{{- if eq .os "darwin" -}}
#!/bin/bash
if [ $# -eq 0 ] || [ $# -gt 2 ]; then
  echo >&2 "Usage: notify-send <title> [message]"
  exit 1
fi
title="$1"
message="${2:-}"
osascript -e "display notification \"${message//\"/\\\"}\" with title \"${title//\"/\\\"}\""
{{ end -}}
