#!/usr/bin/env bash
# Usage: chezmoi-render <directory> [templates...]
# Renders managed templates to a staging directory.
# Use -q/--quiet to suppress output.

set -euo pipefail
shopt -s lastpipe

if ((BASH_VERSINFO[0] < 4)); then
  echo >&2 "bash 4+ required, found $BASH_VERSION"
  exit 1
fi

dest="${1:?usage: chezmoi-render <directory> [templates...]}"
shift

quiet=false
templates=()
for arg in "$@"; do
  case "$arg" in
  -q | --quiet)
    quiet=true
    ;;
  *)
    templates+=("$arg")
    ;;
  esac
done

mkdir -p "$dest"

if [ "$quiet" != true ]; then
  printf 'Output: %s\n' "${dest#"$PWD"/}" >&2
fi

source_dir="$(chezmoi source-path)"
if [ -z "$source_dir" ]; then
  printf 'error: could not determine source directory\n' >&2
  exit 1
fi
prefix="${source_dir#"$PWD"/}"

# Strip prefix from explicit template paths
if [ ${#templates[@]} -gt 0 ]; then
  fixed=()
  for t in "${templates[@]}"; do
    fixed+=("${t#"$prefix"/}")
  done
  templates=("${fixed[@]}")
fi

# Get all templates if none provided (find all .tmpl files in source, excluding config)
if [ ${#templates[@]} -eq 0 ]; then
  cd "$source_dir"
  mapfile -t templates < <(find . \( -name '*.tmpl' -o -name 'modify_*' \) -type f | grep -v '^./.chezmoi.yaml.tmpl$' | sort -u)
fi

if [ ${#templates[@]} -eq 0 ]; then
  exit 0
fi

# Resolve source paths to target paths (use absolute paths)
cd "$source_dir"
sources=()
for t in "${templates[@]}"; do
  sources+=("$source_dir/$t")
done
printf '%s\0' "${sources[@]}" | xargs -0 chezmoi target-path | mapfile -t targets
if [ "${PIPESTATUS[1]}" -ne 0 ]; then
  echo >&2 "error: chezmoi target-path failed for:"
  printf '  %s\n' "${sources[@]}" >&2
  exit 1
fi
if [ "${#targets[@]}" -ne "${#templates[@]}" ]; then
  echo >&2 "error: target-path returned ${#targets[@]} paths for ${#templates[@]} templates"
  exit 1
fi

render() {
  local src="$1"
  local target="$2"

  if [ -z "$src" ] || [ -z "$target" ]; then
    return 1
  fi

  local rel="${target#"$HOME"/}"
  local out="$dest/$rel"
  local base
  base=$(basename "$src")

  mkdir -p "${out%/*}"

  local cmd=(chezmoi execute-template --file "$source_dir/$src")
  local input="/dev/null"

  if [[ "$base" == modify_* ]]; then
    cmd+=(--with-stdin)
    if [ -f "$target" ]; then
      input="$target"
    fi
  fi

  # echo >&2 "render debug: ${cmd[*]} <${input#"$PWD"/}> -> ${out#"$PWD"/}"
  "${cmd[@]}" <"$input" >"$out" && exit_code=0 || exit_code=$?
  if [ "$exit_code" -ne 0 ]; then
    echo >&2 "render failed: exit code $exit_code"
    # ${cmd[*]} <${input#"$PWD"/}> -> ${out#"$PWD"/}
  fi
  return "$exit_code"
}

total=${#templates[@]}

failed=0
for i in "${!templates[@]}"; do
  render "${templates[$i]}" "${targets[$i]}" || ((++failed))
done
if [ "$failed" -gt 0 ]; then
  printf 'error: %d template(s) failed to render\n' "$failed" >&2
  exit 1
fi

if [ "$quiet" != true ]; then
  printf 'Rendered: %d templates\n' "$total" >&2
fi
