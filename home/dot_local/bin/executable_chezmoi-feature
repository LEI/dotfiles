#!/bin/sh
# List and check enabled chezmoi features
#USAGE arg "[action]" help="Action to perform" {
#USAGE   choices "list" "enabled"
#USAGE }
#USAGE flag "-o --output <format>" help="Output format" {
#USAGE   choices "text" "json" "yaml"
#USAGE }
#USAGE arg "[feature]..." help="Features to check"

set -eu

usage() {
  echo "Usage: chezmoi feature [list|enabled] [-h|--help]" >&2
  echo "  [-o|--output=text|json|yaml]" >&2
}

get_format() {
  format=
  case "$output" in
  '' | text) ;;
  json) format="toPrettyJson" ;;
  yaml) format="toYaml" ;;
  *)
    echo "chezmoi package: invalid output format: $output" >&2
    usage
    return 1
    ;;
  esac
  echo "$format"
}

list_features() {
  output="$1"
  shift
  format="$(get_format "$output")"
  if [ -n "$format" ]; then
    chezmoi execute "{{ .features | $format }}"
  else
    chezmoi execute '{{ range $key, $value := .features }}{{ $key }}
{{ end -}}'
  fi
}

list_enabled_features() {
  output="$1"
  shift
  format="$(get_format "$output")"
  # shellcheck disable=SC2016
  if [ -n "$format" ]; then
    chezmoi execute "{{ .features | $format }}"
  else
    chezmoi execute '{{ range $key, $value := .features }}{{ if $value }}{{ $key }}
{{ end }}{{ end -}}'
  fi
}

get_feature() {
  key="$1"
  # # shellcheck disable=SC2016
  # value="$(chezmoi execute "{{ get .features \"$key\" }}")"
  # echo "$value"
  jq ".$key == true" "$HOME/.local/state/chezmoi/features.json"
}

if [ $# -eq 0 ]; then
  # usage
  # exit 0
  set -- list
fi

action=
output=text
while [ $# -gt 0 ]; do
  case "$1" in
  list | enabled)
    action="$1"
    shift
    # continue
    ;;
  -*=*)
    key="${1%%=*}"
    value="${1#*=}"
    shift
    set -- "$key" "$value" "$@"
    ;;
  -o | --output)
    shift
    output="$1"
    shift
    ;;
  -o*)
    output="${1#-o}"
    shift
    ;;
  -h | --help)
    usage
    exit 0
    ;;
  *)
    value="$(get_feature "$1")"
    if [ "$value" = false ]; then
      echo "chezmoi feature: feature disabled: $1" >&2
      exit 1
    elif [ "$value" != true ]; then
      echo "chezmoi feature: unknown value: $1 ($value)" >&2
      exit 2
    fi
    # echo "chezmoi feature: feature enabled: $1" >&2
    shift
    ;;
  esac
done

case "$action" in
'')
  # list_features | wc -l
  ;;
list)
  list_features "$output"
  ;;
enabled)
  list_enabled_features "$output"
  ;;
*)
  echo "chezmoi feature: invalid action: $action" >&2
  usage
  exit 1
  ;;
esac
