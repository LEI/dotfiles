#!/bin/sh

# Re-execute the last command in tmux

set -eu

if [ -n "${TMUX:-}" ]; then
  # Send C-m twice to ensure '!!' is expanded
  tmux send-keys -t:.! '!!' C-m C-m
else
  echo "Unknown environment" >&2
  exit 1
fi
