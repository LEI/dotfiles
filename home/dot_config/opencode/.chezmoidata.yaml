---
services:
  opencode:
    active: true
    feature: opencode
    label: com.opencode
    unit: opencode
    # . ~/.config/secrets.d/opencode.conf
    # curl --fail --show-error --silent --user \"${OPENCODE_SERVER_USERNAME:-opencode}:${OPENCODE_SERVER_PASSWORD}\" http://localhost:4096/global/health
    check: curl --show-error --silent http://localhost:4096
