{{- /* chezmoi:modify-template */ -}}
{{- $data := .chezmoi.stdin | fromIni -}}
{{- $settings := includeTemplate "gtk-settings.yaml.tmpl" . | fromYaml -}}
{{- $_ := $data | setValueAtPath "Settings" (mergeOverwrite (get $data "Settings" | default dict) $settings) -}}
{{ $data | toIni | trim }}
