---
enabled: "{{ .features.carapace }}"
packages:
  # Alternatives:
  # https://github.com/nushell/nu_scripts/tree/main/custom-completions
  # https://github.com/sigoden/argc-completions
  carapace:
    # enabled: '{{ not (eq .osIDLike "debian") }}'
    nameOverwrite:
      arch: carapace-bin
      mise: github:carapace-sh/carapace-bin # Segmentation faul with aqua backend on ARM
      windows: rsteube.Carapace
    type: '{{ eq .osIDLike "arch" | ternary "aur" (not (eq .osIDLike "android" "arch" "darwin" "windows") | ternary "mise" nil) }}'
