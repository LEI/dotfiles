---
enabled: "{{ .features.goose }}"
packages:
  block-goose-cli:
    enabled: '{{ eq .osIDLike "arch" "darwin" "fedora" }}'
    type: brew
  block-goose:
    enabled: '{{ eq .osIDLike "arch" "darwin" "fedora" }}'
    type: cask
