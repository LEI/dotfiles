---
packages:
  # gvim:
  # macvim:
  #   enabled: '{{ and .features.vim (eq .osIDLike "darwin") (not .headless) }}'
  macvim-app:
    enabled: '{{ and .features.vim (eq .osIDLike "darwin") (not .headless) }}'
    type: '{{ eq .osIDLike "darwin" | ternary "cask" nil }}'
  vim:
    enabled: "{{ .features.vim }}"
    nameOverwrite:
      fedora: vim-enhanced # vim-minimal
      windows: vim.vim
    # type: '{{ eq .osIDLike "windows" | ternary "winget" nil }}'
  # TODO:  rpm-ostree override remove nano nano-default-editor --install vim-default-editor
  # vim-default-editor: # Conflicts with nano-default-editor
  #   enabled: '{{ and .features.vim (eq .osIDLike "fedora") (not .headless) }}'
