---
extraPackages:
  # neovide:
  #   condition: and .features.neovim (eq .osIDLike "darwin") (not .headless)
  neovide-app:
    condition: and .features.neovim (eq .osIDLike "darwin") (not .headless)
    type: eq .osIDLike "darwin" | ternary "cask" nil
