---
extraPackages:
  helix:
    condition: ne .osIDLike "debian"
    nameOverwrite:
      windows: helix.helix
    type: eq .osIDLike "windows" | ternary "winget" nil
  helixbinhx:
    condition: eq .osID "arch" # ne .osID "manjaro"
    type: '"aur"' # eq .osIDLike "arch" | ternary "aur" ""
