---
extraPackages:
  claude-code:
    enabled: '{{ and (get .features "ai") (eq .osIDLike "arch" "darwin" "windows") }}'
    nameOverwrite:
      npm: "@anthropic-ai/claude-code"
      windows: Anthropic.Claude
    type: '{{ eq .osIDLike "arch" | ternary "aur" (eq .osIDLike "darwin" | ternary "cask" "npm") }}'
  claude-monitor:
    enabled: '{{ get .features "ai" }}'
    type: python
  # claude:
  #   enabled: '{{ and (get .features "ai") (eq .osIDLike "darwin" "windows") (not .headless) }}'
  #   nameOverwrite:
  #     windows: Anthropic.ClaudeCode
  #   type: '{{ eq .osIDLike "darwin" | ternary "cask" nil }}'
