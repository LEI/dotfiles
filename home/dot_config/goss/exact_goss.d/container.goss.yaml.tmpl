{{- /* Check the configured engine, not an auto-picked live one: the provider is
       a pure config value now, so the check verifies that engine responds */ -}}
{{- if or .features.podman .features.docker -}}
---
command:
  container-engine:
    exec: {{ .containerProvider }} version
    exit-status: 0
    timeout: 5000
{{ end -}}
