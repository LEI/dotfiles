{{- $provider := get .ai.providers "llama-server" -}}
#!/bin/sh

set -eu

. "$CHEZMOI_WORKING_TREE/home/.chezmoitemplates/helpers.sh"

if ! has brew; then
  die "brew not found"
fi
# brew list llama.cpp >/dev/null 2>&1 # llama.cpp not installed

{{ includeTemplate "launchd-apply.sh.tmpl" (merge (dict "enabled" (eq (get .ai "localProvider") "llama-server") "chezmoi" .chezmoi) $provider) -}}
