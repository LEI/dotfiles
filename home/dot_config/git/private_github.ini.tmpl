{{- if and (. | hasKey "git") (.git | hasKey "github") -}}
{{- includeTemplate "gitconfig" (merge .git.github (dict "name" .name)) -}}

{{- if .git.github | get "user" }}

[github]
	user = {{ .git.github.user | quote }}
	token = {{ keyring "github" .git.github.user | quote }}
{{- end }}

# [hub]
# 	protocol = https
{{ end -}}
