{{- if and .features.opencode (lookPath "openssl" | isExecutable) -}}
OPENCODE_URL={{ dig "opencode" "server" "url" "http://localhost:4096" . }}
OPENCODE_SERVER_USERNAME=opencode
OPENCODE_SERVER_PASSWORD={{ output "openssl" "rand" "-hex" "16" | trim }}
{{ end -}}
