---
packages:
  atuin: # ddworken/hishtory, dvorka/hstr
    # name: atuin atuin-sync
    nameOverwrite:
      winget: Atuinsh.Atuin
    enabled: "{{ .features.atuin }}"
    # FIXME: linuxbrew path in non interactive scripts
    # type: '{{ eq .osIDLike "fedora" | ternary "brew" nil }}'
  # ble.sh:
  #   enabled: '{{ ne .osID "alpine" }}'
  #   name: github:akinomyoga/ble.sh
  #   version: 0.4.0-devel3
  #   type: mise
