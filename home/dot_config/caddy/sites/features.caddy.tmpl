{{- range $name, $s := .caddy.sites -}}
{{- if not (get $.features (get $s "feature")) }}{{ continue }}{{ end -}}
{{- $caddy := get $s "caddy" -}}
{{ $name }}.test {
	import common
	tracing {
		span {{ $name }}
	}
	reverse_proxy localhost:{{ $s.port }}{{ if $caddy }} {
		transport http {
			{{- range $key, $value := $caddy }}
			{{ $key | snakecase }} {{ $value }}
			{{- end }}
		}
	}{{ end }}
}
{{ end -}}
