---
- name: Windows roles
  hosts: os_cygwin
  gather_facts: no
  tags: [win]

  roles:

    # Update setup.ini and symlink ~/.minttyrc
    - role: cygwin
      tags: [cyg]

    # Install Cygwin packages with apt-cyg
    - role: package
      tags: [pkg]
      packages: "{{ cygwin_packages }}"
