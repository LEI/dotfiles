---
- name: Local environment
  hosts: local
  gather_facts: no
  tags: [local]

  roles:

    # Bash dotfiles
    - role: shell
      tags: [bash, bin, shell]

    # Misc dotfiles
    - role: home
      tags: [home]

    # Tmux
    - role: tmux
      tags: [tmux]

    # Vim
    - role: vim
      tags: [vim]

    # - role: ssh
    #   tags: [ssh]

    # - role: weechat
    #   tags: [weechat, irc]
