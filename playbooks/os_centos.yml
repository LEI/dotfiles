---
- name: CentOS roles
  hosts: os_centos
  gather_facts: no
  tags: [rhel, centos]

  roles:

    - role: yum
      tags: [yum]

    - role: package
      tags: [pkg]
      packages: "{{ yum_packages }}"
