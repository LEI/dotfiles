---
- name: MacOSX roles
  hosts: os_macosx
  gather_facts: no
  tags: [osx]

  roles:

    - role: homebrew
      tags: [brew, cask]

    - role: package
      tags: [pkg]
      packages: "{{ brew_packages }}"

    - role: cask
      tags: [cask]
      packages: "{{ brew_casks }}"

    # Install node and global npm packages
    - role: node
      tags: [node, npm]
      packages: "{{ npm_packages }}"

    - role: hammerspoon
      tags: [ws, hs]
      files:
        dest: .hammerspoon

    # - role: osx
    #   tags: [defaults]
